version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.11
#    commands:
#      - pip install -r requirements.txt -t .
  build:
    commands:
      - zip -r function.zip .  # 把所有依賴與 main.py 打包成 zip

artifacts:
  files:
    - function.zip
